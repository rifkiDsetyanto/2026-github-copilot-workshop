#!/bin/sh

repo_root=$(git rev-parse --show-toplevel) || exit 1
cd "$repo_root" || exit 1

echo "Running tests before push..."
npm test
status=$?

if [ "$status" -ne 0 ]; then
  echo "Push blocked: npm test failed."
  exit "$status"
fi

echo "Tests passed. Continuing push."
exit 0
